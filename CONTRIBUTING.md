# Contributing

Create short-lived branches, keep hardware constants out of algorithms, and
include tests for protocol or safety changes. Pull requests that can enable an
actuator must state the tested hardware, motor-power isolation method, failure
cases exercised and rollback procedure.

Never commit API keys, personal GPS traces, raw camera recordings, machine-local
serial paths or unlicensed datasets.
